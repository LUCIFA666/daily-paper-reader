---
title: "MAPLE: Multi-State Aggregated Policy Evaluation for AlphaZero in Imperfect-Information Games"
title_zh: MAPLE：面向不完美信息游戏中AlphaZero的多状态聚合策略评估
authors: "Qian-Rong Li, Hung Guei, I-Chen Wu, Ti-Rong Wu"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.24139v1"
tags: ["query:alphazero"]
score: 9.0
evidence: 将AlphaZero扩展到不完美信息游戏
tldr: 不完全信息游戏中玩家无法观察全局状态，现有方法PIMC存在策略融合问题，IS-MCTS计算成本高。本文提出MAPLE，在单一搜索树中聚合多个世界状态的策略与价值评估，结合两者优势并控制成本，通过Siamese采样选择关键世界状态。在Phantom Go和Dark Hex上，MAPLE相对PIMC基线分别提升291和136 Elo。该方法为AlphaZero在不完全信息游戏中的应用提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.24139v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1839, \"height\": 612, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24139v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 743, \"height\": 928, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24139v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 632, \"height\": 791, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24139v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 778, \"height\": 989, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24139v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 739, \"height\": 692, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.24139v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 678, \"height\": 569, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24139v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 765, \"height\": 685, \"label\": \"Table\"}]"
motivation: 现有不完全信息游戏搜索方法PIMC存在策略融合问题，IS-MCTS计算成本高，亟需兼顾效率与效果的树搜索方法。
method: 提出MAPLE，在单个搜索树中聚合多个采样世界状态的策略与价值评估，并利用Siamese采样策略从信息集中选取关键世界状态。
result: 在Phantom Go和Dark Hex中，MAPLE相比PIMC-based AlphaZero分别取得291和136 Elo的提升。
conclusion: MAPLE是AlphaZero在不完全信息游戏中的有效扩展，融合了PIMC与IS-MCTS的优势，具备可控的计算成本。
---

## 摘要
不完美信息游戏（IIG）具有挑战性，因为玩家必须在未完全观察真实游戏状态的情况下做出决策。虽然AlphaZero在完美信息游戏中取得了显著成功，但将其扩展到IIG仍然困难。现有的基于搜索的方法，如完美信息蒙特卡洛（PIMC），存在策略融合问题，而信息集蒙特卡洛树搜索（IS-MCTS）在与神经网络结合时计算成本高昂。本文提出了一种多状态聚合策略评估（MAPLE），这是一种树搜索方法，在单个搜索树内聚合来自多个采样世界状态的策略和价值评估，结合了PIMC和IS-MCTS的优势，同时保持可控的计算成本。我们进一步引入基于孪生网络的采样策略，从信息集中选择信息丰富的世界状态。在Phantom Go和Dark Hex上的实验表明，MAPLE显著优于基于PIMC的AlphaZero基线，分别实现了291和136的Elo提升。这些结果表明，MAPLE是在不完美信息游戏中进行类AlphaZero学习的有效方法。

## 速览
**TLDR**：在不完美信息博弈中，AlphaZero面临策略融合和高计算成本问题。本文提出MAPLE，通过聚合多个抽样世界状态的策略与价值评估，结合PIMC和IS-MCTS优势，并采用孪生网络采样关键状态。在Phantom Go和Dark Hex上，MAPLE分别提升Elo 291和136分，显著优于PIMC基线，为AlphaZero在不完美信息博弈中的扩展提供了有效方案。 \
**Motivation**：现有PIMC方法存在策略融合问题，IS-MCTS与神经网络结合计算成本高，AlphaZero难以直接应用于不完美信息博弈。 \
**Method**：提出MAPLE，在单棵搜索树中聚合多个世界状态的策略和价值评估，并采用孪生网络选择信息集中的关键状态。 \
**Result**：在Phantom Go和Dark Hex游戏中，MAPLE较PIMC基线分别取得291和136的Elo提升。 \
**Conclusion**：MAPLE有效结合多状态聚合与可控计算成本，为AlphaZero在不完美信息博弈中的学习提供了新途径。

---

## Abstract
Imperfect-information games (IIGs) are challenging, as players must make decisions without fully observing the true game state. While AlphaZero has achieved remarkable success in perfect-information games, extending it to IIGs remains difficult. Existing search-based approaches, such as Perfect Information Monte Carlo (PIMC), suffer from strategy fusion, while Information Set Monte Carlo Tree Search (IS-MCTS) incurs high computational cost when combined with neural networks. In this paper, we propose Multi-State Aggregated PoLicy Evaluation (MAPLE), a tree search method that aggregates policy and value evaluations from multiple sampled world states within a single search tree, combining the advantages of PIMC and IS-MCTS while maintaining a controllable computational cost. We further incorporate a Siamese-based sampling strategy to select informative world states from the information set. Experiments on Phantom Go and Dark Hex show that MAPLE significantly outperforms the PIMC-based AlphaZero baseline, achieving Elo improvements of 291 and 136, respectively. These results demonstrate that MAPLE is an effective approach for AlphaZero-style learning in imperfect-information games.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

不完美信息博弈（IIG）中，玩家无法完全观察真实游戏状态，决策需依赖部分观察和推测。AlphaZero 在完美信息游戏中取得突破性成功，但其直接扩展到 IIG 面临两大难题：
- **策略融合**：PIMC（完美信息蒙特卡洛）对每个采样的世界状态独立搜索，然后将结果平均，可能导致最终决策与任何单一世界状态的最优行动不一致。
- **计算成本**：IS-MCTS（信息集蒙特卡洛树搜索）维护单一搜索树以缓解策略融合，但引入神经网络后，每次模拟需沿选择路径评估网络，总评估次数无法精确控制。

现有 AlphaZe**（PIMC+AlphaZero）虽然简单可控，但受策略融合影响；IS-MCTS 则计算成本高且难以与 AlphaZero 集成。因此需一种既能缓解策略融合又能保持可控计算开销的搜索方法。

## 2. 提出的方法论

### 核心思想
**MAPLE**：在单一搜索树中聚合多个采样世界状态的策略（policy）与价值（value）评估，结合 PIMC 的可控成本与 IS-MCTS 的单一树结构，通过额外引入孪生网络（Siamese Network）选择信息集（information set）中更具代表性的世界状态。

### 关键技术细节

**MAPLE 树搜索四阶段**（每步决策执行一次，迭代 N 次）：
- **多状态采样**：从当前信息集中采样 k 个世界状态（可随机或使用孪生网络筛选）。
- **选择**：根据 PUCT 公式在单棵树上选择一条路径，然后将该路径的动作序列应用到每个采样状态，丢弃使动作非法的状态。
- **评估与扩展**：对每个合法到达叶子节点的世界状态，用神经网络预测策略分布 p_i 和价值 v_i。对动作 a_j，按公式 (2) 聚合合法状态的平均策略：  
  $\bar{p}(a_j) = \frac{1}{|W_j|} \sum_{w_i \in W_j} p_i(a_j)$  
  按公式 (3) 聚合平均价值：  
  $\bar{v} = \frac{1}{|W|} \sum_{w_i \in W} v_i$  
  用 $\bar{p}$ 初始化子节点先验，$\bar{v}$ 用于反向传播。
- **反向传播**：将 $\bar{v}$ 沿选择路径回传，更新访问计数和均值。

**孪生网络采样**：
- 候选池大小 m（默认50），选择与锚点（当前信息状态）嵌入距离最小的 k 个世界状态。
- 孪生网络与 AlphaZero 联合训练，使用 triplet loss：  
  $\max(0, 1 + d_{ap} - d_{an})$，其中 $d_{ap}$ 为锚点与真实状态的欧氏距离，$d_{an}$ 为锚点与负样本的距离。

**损失函数**：  
$L = (z - v)^2 - \pi^T \log p + \max(0, 1 + d_{ap} - d_{an}) + c\|\theta\|^2$  
包含价值损失、策略损失、孪生损失和 L2 正则化。

### 计算成本
- 每步网络评估次数：k × N（N 为模拟次数，即 16），加上孪生网络对 m 个候选状态的评估（默认 m=50）。
- 对比：PIMC 同需 k × N 次评估；IS-MCTS 需 N × d（d 为搜索深度，不可控）。

## 3. 实验设计

### 环境
- **Phantom Go**（9×9）：围棋的变体，玩家看不到对手棋子，非法移动由裁判指出。
- **Dark Hex**（11×11）：Hex 的变体，规则类似，反馈仅含非法移动。

### 网络与特征
- 策略-价值网络：3 个残差块，256 个卷积滤波器。
- 输入特征：6 个特征平面（棋盘 + 揭示信息）；孪生网络使用历史观察（8个回合）作为锚点，棋盘特征作为正/负样本。

### 基准与对比方法
所有模型训练 200 次迭代（每次生成 1000 局自对弈，200 步优化），模拟数 N=16。
- **AlphaZe***（基线）：PIMC+AlphaZero，k=5。
- **MAPLE-r**：MAPLE + 随机采样（无孪生）。
- **MAPLE**：MAPLE + 孪生采样。

评估时每个模型使用两个检查点（Phantom Go：5k/15k步；Dark Hex：15k/25k步），每个检查点与对手池（6个对手，包含三个模型各自的检查点）进行 250 局比赛（先手后手交替），共 1500 局/检查点。模拟数提升到 100/步。

### 消融实验
- **采样世界状态数量 k**：训练和评估时取 {1,5,10} 的组合（图3）。
- **候选世界状态数量 m**：训练和评估时取 {10,30,50,100} 的组合（图4）。
- **孪生嵌入演化**：对比训练步骤0/20k/40k 下，不同时期模型对不同时期正样本的嵌入分布（图5）。

## 4. 资源与算力

论文**未明确说明 GPU 型号、数量或训练时长**。仅提及使用开源框架 MiniZero，并给出超参数（表 II）：40000 次训练步骤，每步 1024 批大小，优化器 SGD。可以推断训练规模适中，但无法精确量化算力需求。

## 5. 实验数量与充分性

- **主对比实验**：两个游戏，每组三个模型，每个模型两个检查点，总比赛局数（评估）较多（每个检查点1500局），置信区间合理。
- **消融实验**：对 k 和 m 进行了完整的 3×3 和 4×4 网格搜索（训练与评估组合），共 9+16 组配置，统计了胜率及置信区间。
- **嵌入分析**：直观展示了孪生网络随训练过程适应数据，可信且有意义。
- **公平性**：所有对比方法使用相同模拟数、网络架构、训练数据量；对手池由三者自身构成，避免了引入外部强棋力偏差，但同时也限制了跨方法比较的普适性。

总体实验较充分，覆盖了关键参数空间，结论有统计支持。但仅限于两个棋类游戏，未与其他 IIG 方法（如 CFR、深度 CFR）对比。

## 6. 主要结论与发现

- **MAPLE 显著优于 PIMC 基线**：  
  - Phantom Go：AlphaZe** Elo 1063，MAPLE-r 1209，MAPLE 1354（提升 291 Elo，胜率从 58.96% 增至 88.45%）。  
  - Dark Hex：AlphaZe** 1130，MAPLE-r 1076，MAPLE 1266（提升 136 Elo，胜率从 67.87% 增至 82.20%）。
- **孪生采样在信息集较大时更关键**：随机采样在 Phantom Go 上有提升，但在 Dark Hex 中反而略低于基线，而孪生采样带来显著增益。
- **采样数量 k 的折中**：k=5 在性能和计算之间达到最佳；增大 k 有时反而降低训练效果（Dark Hex 中 k=10 训练较差）。
- **候选池 m 足够大时孪生采样有效**：m=50 通常为最佳，继续增加 m 收益递减。
- **孪生网络能自主学习**：嵌入可视化显示，正样本随训练越来越靠近锚点，且模型能适应自身生成的数据分布变化。

## 7. 优点

- **方法设计巧妙**：聚合多世界状态在同一搜索树中，既缓解策略融合又确保计算成本可预测（k×N 次网络评估 + 额外孪生评估）。
- **孪生采样与训练一体**：无需预训练，能随 AlphaZero 策略演化而适应，提高样本质量。
- **实验全面**：进行了主性能比较、参数消融、嵌入演化分析，统计严谨（置信区间、多局比赛）。
- **可复现性强**：基于开源框架，超参数公开，组件定义清晰。
- **结果突出**：在两个代表性不完美信息游戏中显著超越现有基线，证明了方法的有效性。

## 8. 不足与局限

- **游戏范围狭窄**：仅验证了两个同质化的迷幻版棋类游戏（Phantom Go 和 Dark Hex），未涉及其他类型 IIG（如扑克、桥牌或 Reconnaissance Blind Chess），普适性待检验。
- **对手池自参照**：对手来自模型自身，未与 CFRE 或深度 RL 等 SOTA 方法对比，可能高估绝对强度。
- **额外计算开销**：每步需要评估 m 个候选世界状态的孪生网络（m=50），虽可控但增加成本。
- **策略融合缓解程度未定量分析**：未通过内部度量（如行动一致性）直接验证策略融合的减轻程度。
- **缺少跨方法对比**：未与 IS-MCTS+AlphaZero 或深度 CFR 比较，无法严格说明 MAPLE 比 IS-MCTS 更优（主要对比 PIMC 基线）。
- **训练设置简化**：仅使用 Gumbel AlphaZero 和固定模拟数，可能忽略不同搜索预算下的性能差异。

（完）
