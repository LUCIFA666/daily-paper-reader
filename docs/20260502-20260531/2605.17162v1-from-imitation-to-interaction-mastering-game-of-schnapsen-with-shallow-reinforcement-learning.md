---
title: "From Imitation to Interaction: Mastering Game of Schnapsen with Shallow Reinforcement Learning"
title_zh: 从模仿到交互：用浅层强化学习掌握Schnapsen游戏
authors: "Ján Klačan, Sizhong Zhang"
date: 2026-05-16
pdf: "https://arxiv.org/pdf/2605.17162v1"
tags: ["query:alphazero"]
score: 8.0
evidence: 研究浅层强化学习在纸牌游戏Schnapsen中的应用，属于AlphaZero游戏变体
tldr: 本文探究浅层神经网络能否精通Schnapsen纸牌游戏并挑战强搜索基线RdeepBot。通过渐进实验设计，先评估监督学习代理MLPBot，再评估强化学习代理RLBot。结果表明监督模仿泛化不足，而强化学习显著更强。最佳性能出现在将学习到的价值函数与深度搜索结合时，对最强RdeepBot基线取得统计显著更高胜率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.17162v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1173, \"height\": 976, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.17162v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1370, \"height\": 2041, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.17162v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1515, \"height\": 1583, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.17162v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1550, \"height\": 1742, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.17162v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1244, \"height\": 732, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.17162v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1307, \"height\": 668, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.17162v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1714, \"height\": 852, \"label\": \"Table\"}]"
motivation: 验证浅层神经网络能否在无需深度架构下通过强化学习掌握复杂纸牌游戏并超越强搜索基线。
method: 使用浅层神经网络，分别通过监督学习（MLPBot）和异步蒙特卡洛更新结合经验回放的强化学习（RLBot）进行训练。
result: RLBot在结合深度搜索后对最强RdeepBot基线达到统计显著更高胜率，而监督学习无法击败强对手。
conclusion: 浅层强化学习结合搜索能有效掌握游戏，监督模仿则不足；价值函数与搜索深度协同是关键。
---

## 摘要
本文研究浅层神经网络智能体能否掌握纸牌游戏Schnapsen，并挑战基于强搜索的基线RdeepBot（使用蒙特卡洛采样和前瞻搜索）。在逐步复杂的实验设计指导下，我们首先评估了一个在回放数据上训练的监督学习智能体（MLPBot），然后评估了一个具有相同浅层架构的强化学习智能体（RLBot），该智能体通过异步蒙特卡洛更新和经验回放进行训练。结果表明，监督模仿不能很好地泛化以击败强大的RdeepBot对手，而强化学习产生的智能体明显更强。在关注RdeepBot深度参数的设置中，当学习到的价值函数在游戏过程中与更深入的前瞻相结合时，达到了最佳性能，使RLBot针对评估的最强RdeepBot基线获得了统计上显著更高的胜率。在基于样本的设置中，收益更具条件性：最强的性能出现在相对较低的训练num_samples参数下，而不是随着更强的采样而均匀增加。

## Abstract
This paper investigates whether shallow neural network agents can master the card game Schnapsen and challenge a strong search-based baseline, RdeepBot, which uses Monte Carlo sampling and lookahead search. Guided by a progressively more complex experimental design, we first evaluate a supervised learning agent (MLPBot) trained on replay data and then a reinforcement learning agent (RLBot) with the same shallow architecture trained through asynchronous Monte Carlo updates and experience replay. The results show that supervised imitation does not generalize well enough to defeat strong RdeepBot opponents, whereas reinforcement learning produces substantially stronger agents. In the setting that focuses on the depth parameter of RdeepBot, the best performance is achieved when the learned value function is combined with deeper lookahead during gameplay, allowing RLBot to achieve statistically significant higher winning rates against the strongest evaluated RdeepBot baseline. In the sample-based setting, the gains are more conditional: the strongest performance appears at a relatively lower training num_samples parameter rather than increasing uniformly with stronger sampling.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：Schnapsen 是一种具有不完全信息、动态博弈阶段和复杂策略空间的纸牌游戏。现有的强基线 RdeepBot 通过蒙特卡洛采样和固定深度前瞻搜索来应对部分可观测性，但计算代价高且缺乏长期策略感知。论文旨在探究 **浅层神经网络**（单隐藏层 MLP）能否在不依赖深度架构或显式搜索的情况下，通过 **监督学习** 或 **强化学习** 掌握该游戏，并挑战 RdeepBot。
- **核心问题**：
  - 监督学习（MLPBot，模拟专家回放）能否泛化足够好？
  - 强化学习（RLBot，异步蒙特卡洛更新+经验回放）能否产生更强策略？
  - 在不同搜索深度（depth）和采样数（num_samples）的 RdeepBot 变体下，两种学习范式的表现如何？
- **整体含义**：该工作评估了“浅层学习 vs 搜索”的边界，揭示在特定条件下，浅层强化学习结合有限搜索可达到接近甚至超越强搜索基线的效果，为轻量级游戏 AI 提供思路。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：通过两种学习范式训练同一浅层网络架构（输入层 173 维特征 → 隐藏层 512 个神经元 + ReLU → Sigmoid 输出层），分别验证能力上限。
- **MLPBot（监督学习）**：
  - 输入：状态-动作特征向量（173 维），来自 Schnapsen 引擎的 ml_bot.py。
  - 输出：胜率概率（0/1 二分类）。
  - 训练：使用 RdeepBot 对局（种子固定）生成的 20,000 局回放数据，训练 100 个 epoch，Adam 优化器（lr=5e-4，weight decay=1e-5），Binary Cross Entropy 损失。
  - 决策：贪心选择当前所有合法动作中预测概率最高的动作（无树搜索）。
- **RLBot（强化学习）**：
  - 神经网络架构与 MLPBot 完全相同，但训练方式采用 **异步蒙特卡洛更新 + 经验回放**。
  - 学习动作价值函数 \( Q_\theta(s,a) \in [0,1] \)（预测胜率）。
  - 训练流程：
    - 多个 worker 进程并行进行游戏，记录每个状态-动作特征向量和最终二元奖励（1=胜，0=负）。
    - 将元组推入共享队列，中心缓冲区（FIFO 队列，容量 100,000）存储。
    - 均匀采样 mini-batch（1024 个样本），最小化 MSE 损失：\( L(\theta) = \mathbb{E}[(Q_\theta(s_t,a_t) - G)^2] \)，其中 G 是蒙特卡洛结果。
    - 行为策略为 \(\epsilon\)-greedy，\(\epsilon\) 从 0.23 线性衰减到 0.02，训练 120 万局。
  - 评估时：同样使用贪心选择（无额外搜索），但部分实验中允许在游戏时结合深度前瞻（即使用 RLBot 的价值函数指导深度搜索）。

## 3. 实验设计：数据集 / 场景、基准、对比方法
- **场景与数据集**：Schnapsen 纸牌游戏的模拟环境（Schnapsen Game Engine）。无需外部数据集，回放数据由 RdeepBot 大战自行生成。
- **基准**：
  - RdeepBot（搜索基线），有两个关键参数：**depth**（搜索深度）和 **num_samples**（模拟采样数）。
  - 其他简单基线（RandBot、BullyBot）用于初步验证 RdeepBot 的优越性。
- **主对比方法**：
  - **MLPBot**：三个变体（depth 2, 4, 6 训练的教师对局）。
  - **RLBot-D**：四个变体（训练对手 RdeepBot depth = 2,4,6,8，num_samples 固定 4）。
  - **RLBot-S**：五个变体（训练对手 RdeepBot num_samples = 4,20,40,60,80，depth 固定 4）。
- **评估方式**：每个 bot 变体与每个对手进行 10,000 局比赛，使用单样本 Z 检验（α=0.05）判断胜率是否显著偏离 50%，并分类为 Better/Worse/Equal。

## 4. 资源与算力
- 文中明确指出使用了 **Apple M2 Pro 芯片（10 核）** 和 16GB 统一内存。
- 利用 `Python concurrent.futures` 进行异步 CPU 处理，通常使用 8-9 个核心。
- **未提及 GPU**，所有计算均在 CPU 上完成。
- 训练时长：RLBot 训练 120 万局（具体总时间未给出），MLPBot 训练 100 个 epoch（时间未给出）。
- **注意**：算力描述充分但较为粗略，缺乏精确的训练时间或 GPU 型号（因未使用 GPU）。

## 5. 实验数量与充分性
- **实验数量**：
  - 初步验证：RdeepBot vs RandBot/BullyBot（图1），RdeepBot 不同 num_samples 对比（图2）。
  - 实验1：3 个 MLPBot 变体 × 4 个对手（自身 + 3 个 RdeepBot depth 变体） → 共 12 组比赛（每组 10,000 局）。
  - 实验2：4 个 RLBot-D 变体 × 7 个对手（自身 + 3 个 RdeepBot depth + 其他 RLBot-D 变体？实际图4显示矩阵为 4×7，含自身及其他 RLBot）→ 约 28 组。
  - 实验3：5 个 RLBot-S 变体 × 6 个对手（自身 + 5 个 RdeepBot-S 变体）→ 30 组。
  - 总比赛局数：每组 10,000 局，总计约 70 万局。
- **充分性与公平性**：
  - 所有比赛均使用 10,000 局大样本，统计检验合理。
  - 使用固定种子分配机制避免重复，确保可复现。
  - 控制了基线（RdeepBot）的不同参数（depth, num_samples），分别考察两种学习范式。
  - 消融方面：未做网络层数、容量消融，但已涵盖监督 vs 强化、不同训练对手参数。
  - 局限性：仅测试了浅层网络；未做自我博弈（self-play）或更高级架构比较。

## 6. 论文的主要结论与发现
- **监督学习（MLPBot）**：
  - 无法击败任何 RdeepBot 基线（胜率 28.98%~41.58%），支持假设1。
  - 对手 depth 增加导致胜率下降，但不同训练教师 depth 带来的差异不大。
- **强化学习（RLBot-D，depth 实验）**：
  - 多数 RLBot 变体能显著击败 RdeepBot 基线，支持假设2。
  - 最佳结果：RL-D8 对最强 Rdeep-D8 胜率 57.21%；且更深的游戏搜索（训练后结合 lookahead）带来更高胜率。
  - 支持假设2.1（对手 depth 越强，胜率下降）仅在弱 RL 变体上明显，强变体仍维持优势。
- **强化学习（RLBot-S，sample 实验）**：
  - 结果更具条件性：RL-S20（训练对手 num_samples=20）表现最佳，对所有 RdeepBot-S 变体均显著胜出。
  - 但训练对手采样数超过 20 后（40,60,80），RLBot 性能反而下降，说明并非越强越好，支持假设3.1（更强对手更难击败）但存在阈值。
  - 总体上 RL 优于监督，但样本维度的优势不绝对。
- **核心发现**：浅层强化学习+深度搜索可达到强于纯搜索基线的效果；简单增加训练对手强度未必带来收益。

## 7. 优点：方法或实验设计上的亮点
- **严格的分阶段实验设计**：从监督到强化，逐步提升复杂度，清晰对比两种范式。
- **统计严谨性**：使用 10,000 局比赛和大样本 Z 检验，结果统计显著。
- **并行化实现**：异步 worker 进程 + 确定性种子分配，保证效率与可重复性。
- **参数扫描全面**：针对 RdeepBot 的两个关键参数（depth, num_samples）分别进行实验，揭示其交互影响。
- **可解释性**：通过“Better/Worse/Equal”分类直观呈现结果，并分析胜率趋势。

## 8. 不足与局限
- **架构局限**：仅测试了单隐藏层 MLP（512 神经元），未探索更深或更复杂的网络（如 RNN, Transformer），可能低估了浅层网络能力上限。
- **未采用自我博弈**：RLBot 始终与固定 RdeepBot 训练，未尝试自我对弈（类似 AlphaZero），可能无法发现超越对手的策略。
- **经验回放策略简单**：仅使用均匀采样，未尝试优先经验回放等改进（作者自承局限性）。
- **实验设置简化**：游戏规则中移除了“自愿关闭塔隆”选项，降低了真实游戏复杂度。
- **样本维度的泛化不足**：RLBot-S 的最佳性能仅出现在特定训练对手参数，说明对采样数的鲁棒性差。
- **计算资源报告不够细致**：缺少训练具体耗时、CPU 内存占用等细节。

（完）
